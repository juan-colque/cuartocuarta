

alert("Bienvenia nuetra calculadora");

// Creamos las variables para la SUMA
let numeroUno = prompt("Introduce el primer número");
let numeroDos = prompt("Introduce el segundo número");

// Convertimos en numeros los datos
numeroUno = parseInt(numeroUno);
numeroDos = parseInt(numeroDos);

// Provamos si capturamos los datos 
console.log("Los números que ingresan son" + numeroUno + " y " + numeroDos);

// Creamos la calculadora
console.log("El resultado de la suma es: " + (numeroUno + numeroDos));

